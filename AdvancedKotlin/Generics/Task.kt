package AdvancedKotlin.Generics

// Base interface for tasks, leveraging your OOP knowledge
interface Task {
    val id: Int
    val title: String
    /*
     * Generics Context:
     * - Defines a common interface for tasks, used in generic classes/interfaces.
     * - Java Equivalent: `public interface Task { int getId(); String getTitle(); }`.
     * - From Your Knowledge: Interfaces for polymorphism (OOP, design patterns).
     * - Android Relevance: Models data for UI (e.g., to-do items in a RecyclerView).
     */
}










